{
  "name": "elector-lookup-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "cd web && npm install && npm run build",
    "start": "npm run start --prefix web"
  }
}
